1. Use timestamp for busy status - when setting to busy set as busy for some default time.
	Add a check every x minutes that will update the busy status according to the timestamps
2. Add filtering by team
